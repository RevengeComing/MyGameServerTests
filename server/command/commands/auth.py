from server.command.base import BaseCommand


class Authenticate(BaseCommand):
    def process(self):
        print("New player %s", self.data['username'])

    def update_world():
        self.username = self.data['username']

    def update_player():
        self.username = self.data['username']
